pub fn lshr(x: i64, k: i64) -> i64 = {
    let mut mask: i64 = 1;
    mask = (mask << (64 - k)) - 1;
    return (x >> k) & mask;
}

pub fn main() -> i64 = {
    let x: i64 = -1;
    let y: i64 = lshr(x, 1);
    return y;
}
